"""
cost_function.py

Contains the compute_cost(solution) function, which returns the cost of a solution (all measures applied + penalties for violations, if any).

This function is used by SCIPO and heuristics to evaluate the "quality" of a solution.
"""